spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/rik_test
    username: root
    password: 1234
  mvc:
    hiddenmethod:
      filter:
        enabled: true